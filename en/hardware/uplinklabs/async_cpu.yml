---
cpu_speed_unit: Ghz

Name: CPU ( Async {{ speed }} {{ cpu_speed_unit }} )
DescriptionName: Asynchronous CPU ( {{ speed }} {{ cpu_speed_unit }} )

cpu_speed_20: |
  A slow and simple processor designed for use in parallel with many others in large scale super-computers.
  
  This CPU uses a new asynchronous architecture. Visit our Knowledge Database to learn more.
  
cpu_speed_60: |
  A low budget, low speed processor.
  
  This CPU uses a new asynchronous architecture. Visit our Knowledge Database to learn more.
  
cpu_speed_80: |
  A faster processor with onboard MPU instruction handling for faster instruction execution.
  
  This CPU uses a new asynchronous architecture. Visit our Knowledge Database to learn more.
  
cpu_speed_100: |
  A very fast processor, ideal for cpu-intensive activity,
  
  This CPU uses a new asynchronous architecture. Visit our Knowledge Database to learn more.
  
cpu_speed_120: |
  A new processor from Darwin Research with nano-drive technology, allowing multiple processing components to fit in a single slot.
  
  This CPU uses a new asynchronous architecture. Visit our Knowledge Database to learn more.
  
cpu_speed_150: |
  A new processor from Darwin Research with nano-drive technology, allowing multiple processing components to fit in a single slot.
  
  This CPU uses a new asynchronous architecture. Visit our Knowledge Database to learn more.
  
cpu_speed_200: |
  A new processor from Darwin Research with nano-drive technology, allowing multiple processing components to fit in a single slot.
  
  This CPU uses a new asynchronous architecture. Visit our Knowledge Database to learn more.
  
cpu_speed_240: |
  A new prototypical processor with enhanced nano-drive technology.
  
  This CPU uses a new asynchronous architecture. Visit our Knowledge Database to learn more.

Description: |
  {% if speed == 20 %} {{ cpu_speed_20 }}
  {% elif speed == 60 %} {{ cpu_speed_60 }}
  {% elif speed == 80 %} {{ cpu_speed_80 }}
  {% elif speed == 100 %} {{ cpu_speed_100 }}
  {% elif speed == 120 %} {{ cpu_speed_120 }}
  {% elif speed == 150 %} {{ cpu_speed_150 }}
  {% elif speed == 200 %} {{ cpu_speed_200 }}
  {% elif speed == 240 %} {{ cpu_speed_240 }}
  {% endif %}
