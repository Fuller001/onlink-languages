---
Subject: Change the DNS record for a server
Body: |
    Gain access to {{ target_name }}:
    
    TARGET COMPUTER DATA :
      LOCATION : {{ computer_name }}
      IP : Unknown
    
    Access the DNS record for the following IP and change the primary nameserver.
      IP : {{ target_ip }}
      New Nameserver : {{ misc_string }}
    
    Send a notice of completion to:
    {{ employer_email }}
    
    END
