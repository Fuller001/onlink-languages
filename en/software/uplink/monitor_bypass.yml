---
Name: Monitor Bypass
Type: BypassTool

Description: |
  {% if version == 1.0 %} Actively bypasses a monitor with a security level of 1.
  {% else %} Actively bypasses a monitor with a security level of {{ level }} or below.
  {% endif %}
  All bypass tools require a 'HUD:ConnectionAnalysis' upgrade first.
