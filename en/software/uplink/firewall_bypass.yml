---
Name: Firewall Bypass
Type: BypassTool

Description: |
  {% if version == 1.0 %} Actively bypasses firewall systems with a security level of 1.
  {% else %} Actively bypasses firewall systems with a security level of {{ level }} or below.
  {% endif %}
  All bypass tools require a 'HUD:ConnectionAnalysis' upgrade first.
