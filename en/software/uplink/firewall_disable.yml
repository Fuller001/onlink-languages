---
Name: Firewall Disable
Type: Disabler

Description: |
  {% if version == 1.0 %} Disables a firewall with a security level of 1.
  {% else %} Disables a firewall with a security level of {{ level }} or below.
  {% endif %}
  This action will be detected immediately.
