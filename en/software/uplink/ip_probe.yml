---
Name: IP Probe
Type: Other

Description: |
  {% if version == 1.0 %} Scans a given IP address for security systems and reports the types in use.
  {% elif version == 2.0 %} Scans a given IP address for security systems and reports the types in useand their version information.
  {% else %} Scans a given IP address for security systems and reports the types in use, their version information and their status.
  {% endif %}
