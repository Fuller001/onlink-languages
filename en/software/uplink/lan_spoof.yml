---
Name: LAN Spoof
Type: LAN

Description: |
  Spoofs a system on the network, fooling the network itself into believing you are that system.
  {% if version == 1.0 %} Works on level 1 systems only.
  {% elif version == 2.0 %} Works on level 1 and 2 systems.
  {% else %} Works on all LAN systems.
  {% endif %}
