---
Name: Proxy Disable
Type: Disabler

Description: |
  {% if version == 1.0 %} Disables a proxy with a security level of 1.
  {% else %} Disables a proxy with a security level of {{ level }} or below.
  {% endif %}
  This action will be detected immediately.
