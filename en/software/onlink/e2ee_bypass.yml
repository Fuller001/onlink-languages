---
Name: Active Server Encryption Bypass
Type: BypassTool

Description: |
  {% if version == 1.0 %} Actively bypasses an Active Server Encryption System with a security level of 1.
  {% else %} Actively bypasses an Active Server Encryption System with a security level of {{ level }} or below.
  {% endif %}
  All bypass tools require a 'HUD:ConnectionAnalysis' upgrade first.
