---
Subject: 更改服务器的 DNS 记录
Body: |
    获得 {{ target_name }} 的访问权限：
    
    目标计算机数据：
      位置 : {{ computer_name }}
      IP  : 未知
    
    访问以下 IP 的 DNS 记录并更改主名称服务器。
      IP：{{ target_ip }}
      新名称服务器：{{ misc_string }}
    
    将完成通知发送至：
    {{ employer_email }}
    
    END
